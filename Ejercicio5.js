var num;
num=parseFloat(prompt("Ingrese un numero: "))
if(num>=1 && num<=10)
{
    document.write(num+" esta comprendido entre 1 y 10");
}
else
{
    document.write(num+" no esta comprendido entre 1 y 10");
}